name: Zeeshan-Hybrid-Stacking-Repair-Engine

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build Zeeshan Hybrid Engine
        run: |
          mkdir -p app/src/main/java/com/zeeshan/camera
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/cpp
          
          echo "BUILDING HYBRID STACKING & REPAIR ENGINE..."

          cat << 'EOF' > gradle.properties
          android.useAndroidX=true
          android.enableJetifier=true
          org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
          EOF

          cat << 'EOF' > settings.gradle
          pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
          dependencyResolutionManagement { repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS); repositories { google(); mavenCentral() } }
          include ':app'
          EOF

          cat << 'EOF' > build.gradle
          plugins { 
              id 'com.android.application' version '8.1.0' apply false 
              id 'org.jetbrains.kotlin.android' version '1.8.22' apply false 
          }
          EOF

          # 🛠️ 1. CMAKE ENGINE 
          cat << 'EOF' > app/src/main/cpp/CMakeLists.txt
          cmake_minimum_required(VERSION 3.22.1)
          project("zeeshancamera")
          add_library(zee_hybrid SHARED native-lib.cpp)
          find_library(log-lib log)
          find_library(jnigraphics-lib jnigraphics)
          target_link_libraries(zee_hybrid ${log-lib} ${jnigraphics-lib})
          EOF

          # 🔥 2. THE HYBRID C++ ENGINE (Stacking 5 Frames + Repairing)
          cat << 'EOF' > app/src/main/cpp/native-lib.cpp
          #include <jni.h>
          #include <android/bitmap.h>
          #include <android/log.h>
          #include <vector>
          #include <cmath>
          #include <algorithm>

          #define LOG_TAG "ZeeHybridEngine"
          #define LOGI(...) __android_log_print(ANDROID_LOG_INFO, LOG_TAG, __VA_ARGS__)

          extern "C" JNIEXPORT void JNICALL
          Java_com_zeeshan_camera_AiDriver_processHybridStack(JNIEnv* env, jobject /* this */, jobjectArray bitmaps, jobject outputBitmap) {
              int num_frames = env->GetArrayLength(bitmaps);
              AndroidBitmapInfo info;
              AndroidBitmap_getInfo(env, outputBitmap, &info);
              
              int width = info.width;
              int height = info.height;
              
              std::vector<void*> pixel_ptrs(num_frames);
              for (int i = 0; i < num_frames; i++) {
                  jobject bmp = env->GetObjectArrayElement(bitmaps, i);
                  AndroidBitmap_lockPixels(env, bmp, &pixel_ptrs[i]);
              }

              void* out_pixels;
              AndroidBitmap_lockPixels(env, outputBitmap, &out_pixels);

              LOGI("Hybrid Engine: Stacking %d frames and applying Remaster...", num_frames);

              // 🧠 STEP 1: STACKING (Pixel Averaging)
              for (int y = 0; y < height; y++) {
                  uint32_t* out_line = (uint32_t*)out_pixels;
                  for (int x = 0; x < width; x++) {
                      int r_sum = 0, g_sum = 0, b_sum = 0;
                      for (int i = 0; i < num_frames; i++) {
                          uint32_t pixel = ((uint32_t*)pixel_ptrs[i])[x];
                          r_sum += (pixel >> 16) & 0xff;
                          g_sum += (pixel >> 8) & 0xff;
                          b_sum += pixel & 0xff;
                      }
                      
                      // Average value (Noise Reduction)
                      int r = r_sum / num_frames;
                      int g = g_sum / num_frames;
                      int b = b_sum / num_frames;

                      // 🧠 STEP 2: REPAIR (Contrast & Sharpness Boost)
                      r = (int)((r - 128) * 1.2 + 128 + 10);
                      g = (int)((g - 128) * 1.2 + 128 + 10);
                      b = (int)((b - 128) * 1.2 + 128 + 10);

                      r = std::max(0, std::min(255, r));
                      g = std::max(0, std::min(255, g));
                      b = std::max(0, std::min(255, b));

                      out_line[x] = (0xFF << 24) | (r << 16) | (g << 8) | b;
                  }
                  out_pixels = (uint32_t*)((char*)out_pixels + info.stride);
                  for (int i = 0; i < num_frames; i++) {
                      pixel_ptrs[i] = (uint32_t*)((char*)pixel_ptrs[i] + info.stride);
                  }
              }

              // Unlock everything
              AndroidBitmap_unlockPixels(env, outputBitmap);
              for (int i = 0; i < num_frames; i++) {
                  jobject bmp = env->GetObjectArrayElement(bitmaps, i);
                  AndroidBitmap_unlockPixels(env, bmp);
              }
          }
          EOF

          # 📦 3. APP BUILD CONFIGURATION
          cat << 'EOF' > app/build.gradle
          plugins { 
              id 'com.android.application' 
              id 'org.jetbrains.kotlin.android'
          }
          android {
              namespace 'com.zeeshan.camera'
              compileSdk 34
              defaultConfig {
                  applicationId 'com.zeeshan.camera'
                  minSdk 24
                  targetSdk 34
                  versionCode 14
                  versionName '14.0-Hybrid-Stack'
                  externalNativeBuild { cmake { cppFlags "-O3" } } 
              }
              externalNativeBuild { cmake { path "src/main/cpp/CMakeLists.txt"; version "3.22.1" } }
              kotlinOptions { jvmTarget = '1.8' }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              def camerax_version = '1.3.1'
              implementation "androidx.camera:camera-core:${camerax_version}"
              implementation "androidx.camera:camera-camera2:${camerax_version}"
              implementation "androidx.camera:camera-lifecycle:${camerax_version}"
              implementation "androidx.camera:camera-view:${camerax_version}"
          }
          EOF

          cat << 'EOF' > app/src/main/res/values/strings.xml
          <resources><string name="app_name">Zeeshan Hybrid 5X</string></resources>
          EOF

          cat << 'EOF' > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.CAMERA" />
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
              <application android:label="@string/app_name" android:theme="@style/Theme.AppCompat.Light.NoActionBar">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # ⚡ 4. KOTLIN DRIVER
          cat << 'EOF' > app/src/main/java/com/zeeshan/camera/AiDriver.kt
          package com.zeeshan.camera
          import android.graphics.Bitmap

          class AiDriver {
              companion object {
                  init { System.loadLibrary("zee_hybrid") } 
              }
              external fun processHybridStack(bitmaps: Array<Bitmap>, outputBitmap: Bitmap)
          }
          EOF

          # 📱 5. MAIN ACTIVITY (Taking 5 Burst Photos)
          cat << 'EOF' > app/src/main/java/com/zeeshan/camera/MainActivity.kt
          package com.zeeshan.camera
          
          import android.Manifest
          import android.annotation.SuppressLint
          import android.content.ContentValues
          import android.content.pm.PackageManager
          import android.graphics.Bitmap
          import android.graphics.BitmapFactory
          import android.graphics.Color
          import android.graphics.Typeface
          import android.os.Bundle
          import android.provider.MediaStore
          import android.view.Gravity
          import android.view.MotionEvent
          import android.view.View
          import android.widget.*
          import androidx.appcompat.app.AppCompatActivity
          import androidx.camera.core.*
          import androidx.camera.lifecycle.ProcessCameraProvider
          import androidx.camera.view.PreviewView
          import androidx.core.app.ActivityCompat
          import androidx.core.content.ContextCompat
          import java.text.SimpleDateFormat
          import java.util.*
          import java.util.concurrent.Executors
          
          class MainActivity : AppCompatActivity() {
              private var imageCapture: ImageCapture? = null
              private lateinit var previewView: PreviewView
              private lateinit var statusText: TextView
              private lateinit var processingOverlay: LinearLayout
              private var cameraControl: CameraControl? = null
              private val cameraExecutor = Executors.newSingleThreadExecutor()
              private val aiDriver = AiDriver()
              private val BURST_COUNT = 5

              override fun onCreate(savedInstanceState: Bundle?) {
                  super.onCreate(savedInstanceState)
                  if (ContextCompat.checkSelfPermission(this, Manifest.permission.CAMERA) == PackageManager.PERMISSION_GRANTED) {
                      initCameraUI() 
                  } else {
                      ActivityCompat.requestPermissions(this, arrayOf(Manifest.permission.CAMERA), 10)
                  }
              }

              override fun onRequestPermissionsResult(rc: Int, perms: Array<String>, results: IntArray) {
                  super.onRequestPermissionsResult(rc, perms, results)
                  if (results.isNotEmpty() && results[0] == PackageManager.PERMISSION_GRANTED) initCameraUI() 
              }

              @SuppressLint("ClickableViewAccessibility")
              private fun initCameraUI() {
                  val root = RelativeLayout(this).apply { setBackgroundColor(Color.BLACK) }
                  previewView = PreviewView(this)
                  root.addView(previewView, -1, -1)

                  val brandText = TextView(this).apply { text = "ZEESHAN HYBRID 5-STACK"; setTextColor(Color.WHITE); textSize = 22f; typeface = Typeface.DEFAULT_BOLD }
                  root.addView(brandText, RelativeLayout.LayoutParams(-2, -2).apply { addRule(RelativeLayout.CENTER_HORIZONTAL); topMargin = 50 })

                  val zoomSlider = SeekBar(this).apply { max = 90 }
                  root.addView(zoomSlider, RelativeLayout.LayoutParams(-1, 150).apply { topMargin = 150 })

                  val captureBtn = Button(this).apply { text = "CREATE MASTERPIECE (5X)"; setBackgroundColor(Color.parseColor("#00E676")); setTextColor(Color.BLACK); textSize = 16f; typeface = Typeface.DEFAULT_BOLD }
                  root.addView(captureBtn, RelativeLayout.LayoutParams(600, 200).apply { addRule(RelativeLayout.ALIGN_PARENT_BOTTOM); addRule(RelativeLayout.CENTER_HORIZONTAL); bottomMargin = 100 })

                  statusText = TextView(this).apply { setTextColor(Color.YELLOW); gravity = Gravity.CENTER; textSize = 16f }
                  root.addView(statusText, RelativeLayout.LayoutParams(-1, 100).apply { addRule(RelativeLayout.ABOVE, captureBtn.id); bottomMargin = 320 })

                  processingOverlay = LinearLayout(this).apply { orientation = LinearLayout.VERTICAL; gravity = Gravity.CENTER; setBackgroundColor(Color.parseColor("#E6000000")); visibility = View.GONE }
                  processingOverlay.addView(TextView(this).apply { text = "ZEESHAN HYBRID ENGINE\nStacking & Repairing Pixels..."; setTextColor(Color.CYAN); textSize = 20f; gravity = Gravity.CENTER })
                  root.addView(processingOverlay, -1, -1)

                  setContentView(root)
                  startCamera()

                  zoomSlider.setOnSeekBarChangeListener(object : SeekBar.OnSeekBarChangeListener {
                      override fun onProgressChanged(s: SeekBar?, p: Int, f: Boolean) {
                          val zl = 1.0f + (p / 10.0f); cameraControl?.setZoomRatio(zl); statusText.text = "Zoom: " + zl + "x"
                      }
                      override fun onStartTrackingTouch(s: SeekBar?) {}
                      override fun onStopTrackingTouch(s: SeekBar?) {}
                  })

                  captureBtn.setOnClickListener { 
                      processingOverlay.visibility = View.VISIBLE
                      captureHybridBurst()
                  }
              }

              private fun startCamera() {
                  val future = ProcessCameraProvider.getInstance(this)
                  future.addListener({
                      val provider = future.get()
                      imageCapture = ImageCapture.Builder().setTargetResolution(android.util.Size(3840, 2160)).setCaptureMode(ImageCapture.CAPTURE_MODE_MAXIMIZE_QUALITY).build()
                      provider.unbindAll()
                      val cam = provider.bindToLifecycle(this, CameraSelector.DEFAULT_BACK_CAMERA, Preview.Builder().build().also { it.setSurfaceProvider(previewView.surfaceProvider) }, imageCapture)
                      cameraControl = cam.cameraControl
                      statusText.text = "Hybrid Stacking Ready"
                  }, ContextCompat.getMainExecutor(this))
              }

              private fun captureHybridBurst() {
                  val frames = mutableListOf<Bitmap>()
                  for (i in 1..BURST_COUNT) {
                      imageCapture?.takePicture(cameraExecutor, object : ImageCapture.OnImageCapturedCallback() {
                          override fun onCaptureSuccess(image: ImageProxy) {
                              try {
                                  val buffer = image.planes[0].buffer
                                  val bytes = ByteArray(buffer.capacity())
                                  buffer.get(bytes)
                                  val bitmap = BitmapFactory.decodeByteArray(bytes, 0, bytes.size, null)
                                  frames.add(bitmap)
                                  
                                  if (frames.size == BURST_COUNT) {
                                      runOnUiThread { statusText.text = "Fusing 5 Frames..." }
                                      val out = frames[0].copy(Bitmap.Config.ARGB_8888, true)
                                      aiDriver.processHybridStack(frames.toTypedArray(), out)
                                      savePhoto(out)
                                  }
                              } finally { image.close() }
                          }
                          override fun onError(e: ImageCaptureException) { runOnUiThread { processingOverlay.visibility = View.GONE } }
                      })
                      Thread.sleep(150)
                  }
              }

              private fun savePhoto(bitmap: Bitmap) {
                  val ts = SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US).format(Date())
                  val cv = ContentValues().apply {
                      put(MediaStore.MediaColumns.DISPLAY_NAME, "ZEESHAN_HYBRID_" + ts + ".jpg")
                      put(MediaStore.MediaColumns.MIME_TYPE, "image/jpeg")
                      put(MediaStore.Images.Media.RELATIVE_PATH, "Pictures/ZeeshanCamera")
                  }
                  val uri = contentResolver.insert(MediaStore.Images.Media.EXTERNAL_CONTENT_URI, cv)
                  uri?.let { contentResolver.openOutputStream(it)?.use { out -> bitmap.compress(Bitmap.CompressFormat.JPEG, 100, out) } }
                  runOnUiThread { 
                      processingOverlay.visibility = View.GONE
                      Toast.makeText(baseContext, "5-Frame Masterpiece Saved!", Toast.LENGTH_LONG).show() 
                  }
              }
          }
          EOF

      - name: Build Hybrid App
        uses: gradle/gradle-build-action@v3
        with:
          arguments: assembleDebug
          gradle-version: 8.1

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Zeeshan-Camera-Hybrid-5X
          path: app/build/outputs/apk/debug/*.apk

